import s from "./customButton.moddule.scss";

export const customButton = () => {
  return (
    <>
      {/* <button
        className={s.inputBtn}
        onClick={() => dispatch(fetchFindCity(value))}
      >
        Add
      </button> */}
    </>
  );
};
